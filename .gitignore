# dependencies and build output
node_modules/
dist/
build/
.next/
.vite/

# local runtime
.env
.env.*
*.local
*.log
.DS_Store
.dev.vars
wrangler.local.*
wrangler.personal.*
.wrangler/

# deployment overlays and infrastructure state
deploy/personal/
infra/**/personal/
*.tfvars
*.tfvars.json
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# private tiering overlay (personal specifics; loaded at runtime from OUTSIDE
# the repo — belt-and-suspenders so it can never be accidentally tracked)
tiering-private-ruleset.json

# local planning notes. Working documents, not repository deliverables: they
# carry absolute local paths and profile layout, and an untracked file at the
# repo root is one `git add -A` away from a public commit.
ATLAS-REWRITE-PLAN.md
*-PLAN.local.md

# graph-derived or private data
fixtures/private/
generated/
cache/
data/
.living-atlas/
*.sqlite
*.sqlite3
*.duckdb
*.parquet
.mcp.json
.codex/
.living-atlas.deploy.wrangler.jsonc
